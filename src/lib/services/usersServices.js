class usersServices {
  //   async addGameIntoUserById(userId, gameId) {
  //     await User.updateOne({ _id: userId }, { $push: { games: gameId } });
  //   }
}

// eslint-disable-next-line import/no-anonymous-default-export
export default new usersServices();
